Task 3 - Docker - NTP
Mission preparation:
Write a Dockerfile that configures NTP.
Script the entry point.
Write a configuration file for NTP.
Execution method:
Use the Dockerfile to read and run the entry point script in the specified directory.
Use the Dockerfile to read and add the NTP configuration file in the specified directory.
So as to achieve the purpose of configuring the NTP server.
Task check:
A mistake was made when writing the NTP configuration file, resulting in a configuration failure.
When building the NTP dockerfile, the 7th step prompts that it cannot be read. Later, it was changed to read successfully.
Result validation:
Create dockerfile and run successfully.
Server Status: Running
